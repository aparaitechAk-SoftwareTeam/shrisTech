import 'package:flutter/material.dart';

class RetailerDashboardPlaceholder extends StatelessWidget {
  const RetailerDashboardPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Retailer Dashboard'),
      ),
    );
  }
}
